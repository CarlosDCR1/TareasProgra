//Incluye el archivo "Notas.h" que contiene los atributos.
#include "Notas.h"

int main() {
    srand(getpid());
    // Llama a la funcion llamaCiclo que inicia el ciclo del programa.
    llamaCiclo();
    return 0;
}

